# ✅ Ignora directorio de plugins, binarios, etc.
.terraform/
*.tfplan
*.backup
crash.log
.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.backup


# ⚠️ PERO NO ignores el estado si usas backend local
# terraform.tfstate  ← ❌ ¡NO lo ignores si necesitas usar destroy!
# terraform.tfstate.* ← ❌